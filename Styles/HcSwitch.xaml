<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="clr-namespace:HCTheme.Controls"
                    >

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="DefaultDark.xaml" />

    </ResourceDictionary.MergedDictionaries>  
    <Style TargetType="{x:Type hc:HcSwitch}" >

        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDark}" />
        <Setter Property="Background" Value="{DynamicResource BackgroundLight}" />
        <Setter Property="Foreground"
                Value="{DynamicResource TextNormal}" />
        <Setter Property="MinHeight" Value="22"/>
        <Setter Property="FontFamily"
                Value="{DynamicResource MSFont}" />
        <Setter Property="FontSize"
                Value="{DynamicResource FontSize12}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate  TargetType="{x:Type hc:HcSwitch}">
                    <Border x:Name="PART_ButtonBase"
                            Margin="{TemplateBinding Padding}"
                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            Background="Transparent">
                        <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="mainboder"
                                    Grid.Column="0"
                                    Width="40"
                                    Height="10"
                                    Margin="1"
                                    Background="{DynamicResource BackgroundDark}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="5"
                                    Visibility="Visible">
                                
                                <Border x:Name="PART_Toggle"
                                        Width="20"
                                        Height="16"
                                        Margin="-1,-4,-1,-4"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        BorderBrush="{DynamicResource BorderDark}"
                                        BorderThickness="1"
                                        Background="{DynamicResource BackgroundLight}"                                        
                                        CornerRadius="6" />
                            </Border>
                            <TextBlock x:Name="onText"
                                       Grid.Column="1"
                                       VerticalAlignment="Center"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       Foreground="{TemplateBinding Foreground}"
                                       IsHitTestVisible="False"
                           Visibility="Hidden"
                                       Text="{TemplateBinding OnText }"
                                        />
                            <TextBlock x:Name="offText"
                                       Grid.Column="1"
                                       VerticalAlignment="Center"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       Foreground="{TemplateBinding Foreground}"
                                       IsHitTestVisible="False"
                                      Visibility="Visible"
                                       Text="{TemplateBinding OffText}"
                                        />

                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource DisableLight }" />
                            <Setter Property="Background" Value="{DynamicResource DisableDark}" />
                            <Setter TargetName="PART_Toggle"
                                    Property="Background"
                                    Value="{DynamicResource DisableLight}" />
                        </Trigger>                       

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="PART_Toggle" Property="HorizontalAlignment" Value="Left" />

                            <Setter TargetName="PART_Toggle"
                                    Property="Background"
                                    Value="{DynamicResource BlueDefault}" />
                            <Setter TargetName="onText" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="offText" Property="Visibility" Value="Hidden"/>
                           
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>
        </Setter>





    </Style>
    
    
    
    

    

</ResourceDictionary>
    
